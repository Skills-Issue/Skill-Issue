import UserMenu from "./UserMenu";

export default function UserMenuOuter(){
    // const logged = await getServerSession(authConfig);
    
    // if (logged){
    //     return(<UserMenu></UserMenu>)
    // }

    return (<UserMenu></UserMenu>)
}